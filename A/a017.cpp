#include <bits/stdc++.h>
using namespace std;

int solve(int h, int len);
string eq;

int main(){
    while(getline(cin, eq)){
        cout << solve(0, eq.length());
    }
    return 0;
}

int solve(int h, int len){

}
